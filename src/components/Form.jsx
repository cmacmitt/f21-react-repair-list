const Form = () => {
  return (
    <form>
      <input
        className="new-repair"
        placeholder="What needs to be repaired?"
        autoFocus=""
      />
    </form>
  );
};

export default Form;
